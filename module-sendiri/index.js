function pangkatDua(angka) {
    return angka * angka
}

function pangkatTiga(angka) {
    return angla * angka * angka
}

function nama() {
    return nama
}

// kalau mau export beberapa file dalam 
// satu kali eksport, gunakan module.eksports = {module yang mau di eksport1 dan module eksport2}
// ini penulisan common JS
module.exports = { pangkatTiga, pangkatDua, nama}